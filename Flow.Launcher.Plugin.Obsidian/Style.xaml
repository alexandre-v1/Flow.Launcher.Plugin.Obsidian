<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:m="http://schemas.modernwpf.com/2019"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/Flow.Launcher.Plugin.Obsidian;component/FlowStyle.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <m:StaticResource x:Key="AccentCardItemBackground" ResourceKey="AccentButtonBackground" />
    <m:StaticResource x:Key="AccentCardItemBackgroundMouseOver" ResourceKey="SystemControlHighlightListLowBrush" />
    <m:StaticResource x:Key="AccentCardItemBackgroundSelected2" ResourceKey="AccentButtonBackgroundPressed" />
    <m:StaticResource x:Key="AccentCardItemBackgroundSelected" ResourceKey="SystemControlHighlightListAccentMediumLowBrush" />
    <m:StaticResource x:Key="AccentCardItemBackgroundSelectedMouseOver" ResourceKey="SystemControlHighlightListAccentMediumBrush" />
    <m:StaticResource x:Key="AccentCardItemForeground" ResourceKey="SystemControlForegroundBaseHighBrush" />
    <m:StaticResource x:Key="AccentCardItemForegroundMouseOver" ResourceKey="AccentButtonForegroundPointerOver" />
    <m:StaticResource x:Key="AccentCardItemForegroundSelected" ResourceKey="SystemControlHighlightAltBaseHighBrush" />

    <m:StaticResource x:Key="AccentCardItemForegroundSecondary" ResourceKey="SystemControlForegroundChromeGrayBrush" />
    <m:StaticResource x:Key="AccentCardItemForegroundSecondaryMouseOver" ResourceKey="SystemControlForegroundChromeGrayBrush" />
    <m:StaticResource x:Key="AccentCardItemForegroundSecondarySelected" ResourceKey="SystemControlHighlightAltBaseMediumBrush" />

    <m:StaticResource x:Key="AccentCardItemBorderBrush" ResourceKey="AccentButtonBorderBrush" />
    <m:StaticResource x:Key="AccentCardItemBorderBrushSelected" ResourceKey="AccentButtonBorderBrushPressed" />
    <m:StaticResource x:Key="AccentCardItemBorderBrushMouseOver" ResourceKey="AccentButtonBorderBrushPointerOver" />
    <m:StaticResource x:Key="AccentCardItemBorderBackgroundMouseOver" ResourceKey="AccentButtonBorderBGPointerOver" />
    <m:StaticResource x:Key="AccentCardItemBorderBackgroundSelected" ResourceKey="AccentButtonBorderBGPressed" />
    <m:StaticResource x:Key="AccentCardItemFocusVisualPrimaryBrush" ResourceKey="SystemControlFocusVisualPrimaryBrush" />
    <m:StaticResource x:Key="AccentCardItemFocusVisualSecondaryBrush" ResourceKey="SystemControlFocusVisualSecondaryBrush" />
    <m:StaticResource x:Key="AccentCardItemCheckBrush" ResourceKey="SystemControlForegroundBaseMediumHighBrush" />

    <!--  Plugin local styles  -->
    <FontFamily x:Key="SegoeFluentIcons">Segoe Fluent Icons, /Resources/#Segoe Fluent Icons</FontFamily>

    <Style x:Key="Title" TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{DynamicResource Color05B}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="LineHeight" Value="36" />
        <Setter Property="FontSize" Value="28" />
    </Style>

    <Style x:Key="Subtitle" TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{DynamicResource Color05B}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="FontSize" Value="20" />
        <Setter Property="LineHeight" Value="28" />
    </Style>

    <Style x:Key="BodyStrong" TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{DynamicResource Color05B}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="LineHeight" Value="20" />
    </Style>

    <Style x:Key="Body" TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{DynamicResource Color05B}" />
        <Setter Property="FontWeight" Value="Regular" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="LineHeight" Value="20" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>

    <Style x:Key="Caption" TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{DynamicResource Color04B}" />
        <Setter Property="FontWeight" Value="Regular" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="LineHeight" Value="16" />
        <Setter Property="TextWrapping" Value="WrapWithOverflow" />
    </Style>

    <Style x:Key="RemoveFromButton" TargetType="Button">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <m:FontIcon
                        x:Name="FontIcon"
                        Foreground="{DynamicResource Color05B}"
                        Glyph="&#xECC9;" />

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="FontIcon" Property="Foreground" Value="{DynamicResource Color10B}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="FontIcon" Property="Foreground" Value="{DynamicResource Color11B}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="CardListBoxItem" TargetType="{x:Type ListBoxItem}">
        <Setter Property="Visibility" Value="Visible" />
        <Setter Property="Foreground" Value="{DynamicResource AccentCardItemForeground}" />
        <Setter Property="Background" Value="{DynamicResource Color00B}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Color03B}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Margin" Value="0,5,0,0" />
        <Setter Property="Padding" Value="0,15,0,15" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                    <Border
                        x:Name="Border"
                        Padding="{TemplateBinding Padding}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="5">
                        <ContentPresenter
                            x:Name="ContentPresenter"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            Focusable="False"
                            RecognizesAccessKey="True"
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                            TextElement.Foreground="{TemplateBinding Foreground}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource AccentCardItemBackgroundSelected}" />
                            <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource AccentCardItemBackgroundSelected}" />
                            <Setter TargetName="ContentPresenter" Property="TextElement.Foreground" Value="{DynamicResource AccentCardItemForegroundSelected}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource AccentCardItemBackgroundMouseOver}" />
                            <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource AccentCardItemBorderBrushMouseOver}" />
                            <Setter TargetName="ContentPresenter" Property="TextElement.Foreground" Value="{DynamicResource AccentCardItemForegroundMouseOver}" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True" />
                                <Condition Property="IsMouseOver" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource AccentCardItemBackgroundSelectedMouseOver}" />
                            <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource AccentCardItemBackgroundSelectedMouseOver}" />
                            <Setter TargetName="ContentPresenter" Property="TextElement.Foreground" Value="{DynamicResource AccentCardItemForegroundSelected}" />
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="CardListBox" TargetType="{x:Type ListBox}">
        <Setter Property="ItemContainerStyle" Value="{StaticResource CardListBoxItem}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>
</ResourceDictionary>
