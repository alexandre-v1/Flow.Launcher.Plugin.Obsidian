<UserControl
    x:Class="Flow.Launcher.Plugin.Obsidian.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="clr-namespace:Flow.Launcher.Plugin.Obsidian.Views"
    xmlns:vm="clr-namespace:Flow.Launcher.Plugin.Obsidian.ViewModels"
    d:DataContext="{d:DesignInstance vm:SettingsViewModel}"
    d:DesignWidth="550"
    Unloaded="OnUnloaded"
    Loaded="OnLoaded"
    mc:Ignorable="d">


    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Flow.Launcher.Plugin.Obsidian;component/Style.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <!-- Top Border is added by Flow Launcher -->
        <Border
            Grid.Row="0"
            Width="Auto"
            Height="Auto"
            Margin="0"
            Padding="0 0 0 10"
            BorderThickness="0"
            CornerRadius="0"
            Style="{StaticResource SettingGroupBox}">

            <StackPanel
                Margin="{StaticResource SettingPanelMargin}"
                Orientation="Vertical">

                <TextBlock
                    Text="Vaults"
                    Margin="0, 0, 0, 15"
                    Style="{DynamicResource SettingTitleLabel}" />

                <views:VaultsLists DataContext="{Binding VaultsListsViewModel}" />
            </StackPanel>
        </Border>

        <Border
            Grid.Row="2"
            Width="Auto"
            Height="Auto"
            Margin="0"
            Padding="0"
            BorderThickness="0, 1, 0 , 0"
            CornerRadius="0"
            Style="{StaticResource SettingGroupBox}">

            <StackPanel Grid.Row="2">
                <Button
                    Name="RefreshPlugin"
                    Margin="20"
                    HorizontalAlignment="Right"
                    Content="Refresh Plugin" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
